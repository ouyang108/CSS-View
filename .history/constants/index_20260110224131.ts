import { isMac } from '@/utils'

export const local_CONFIG = 'local:inspectConfig'
export const default_CONFIG = {
  borderColor: '#000000',
  borderStyle: 'solid',
  // Ctrl+Shift
  keyConfig: 'Control',
}
